#include "main.h"
/**
 * mul - multiplie variables
 *@a: first variable to multiply
 *@b: second variable to multiply
 * Return: result
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
