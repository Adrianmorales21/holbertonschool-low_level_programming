#include "main.h"
/**
 * rev_string -reverse a string
 * @s- string to reverse
 */
void rev_string(char *s)
{
	int len; 
        int i = len;
	char temp;

	 for (len = 0;  *s != '\0'; l++, s++)


        for (i - 1; i >= len / 2; i--);
	{
		temp = i;

}
