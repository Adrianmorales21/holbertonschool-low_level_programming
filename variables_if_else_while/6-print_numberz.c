#include <stdio.h>
/**
 * main-single digit numbers starting from 10
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{putchar(n);
	}
	putchar('\n');
		return (0);
}
